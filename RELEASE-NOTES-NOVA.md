# LX Plus R8 — LX Storage automático

- Novo botão **Gerar endpoint automaticamente** em ADM → Mídia & Upload.
- Integração com o fluxo oficial **Deploy to Cloudflare**.
- Template do Worker agora fica pronto para deploy dentro de `cloudflare-worker/`.
- Worker ganhou modo **public-drive**, sem Service Account, para vídeos compartilhados como “Qualquer pessoa com o link”.
- Service Account continua opcional para arquivos privados.
- Status do ADM diferencia **Drive por link** e **Drive privado**.
- Build/cache atualizado para `NOVA-20260924-R8-LX-STORAGE-AUTO`.
