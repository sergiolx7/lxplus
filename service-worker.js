const CACHE='lxplus-shell-v3210';
const CORE=['./','./index.html','./lxplus.bundle.css?v=32.1','./lxplus.bundle.js?v=32.1','./manifest.webmanifest','./assets/lxplus-logo-v27.png?v=32.1','./assets/icon-v27.svg','./assets/lx-music-fallback.svg'];
self.addEventListener('install',event=>{event.waitUntil((async()=>{const cache=await caches.open(CACHE);await Promise.allSettled(CORE.map(x=>cache.add(x)));await self.skipWaiting()})())});
self.addEventListener('activate',event=>{event.waitUntil((async()=>{for(const key of await caches.keys())if(key!==CACHE&&key.startsWith('lxplus-'))await caches.delete(key);await self.clients.claim()})())});
self.addEventListener('message',event=>{if(event.data==='SKIP_WAITING')self.skipWaiting()});
async function networkFirst(req,timeout=4500){const cache=await caches.open(CACHE),ctrl=new AbortController(),tm=setTimeout(()=>ctrl.abort(),timeout);try{const res=await fetch(req,{signal:ctrl.signal,cache:'no-store'});clearTimeout(tm);if(res&&res.ok)cache.put(req,res.clone()).catch(()=>{});return res}catch(e){clearTimeout(tm);return (await cache.match(req))||(await cache.match('./index.html'))}}
self.addEventListener('fetch',event=>{const req=event.request;if(req.method!=='GET')return;const url=new URL(req.url);if(url.origin!==self.location.origin)return;if(req.mode==='navigate'){event.respondWith(networkFirst(req));return}if(url.pathname.endsWith('/lxplus.bundle.css')||url.pathname.endsWith('/lxplus.bundle.js')){event.respondWith(networkFirst(req,5000));return}event.respondWith(caches.match(req).then(hit=>hit||fetch(req).then(res=>{if(res&&res.ok)caches.open(CACHE).then(c=>c.put(req,res.clone())).catch(()=>{});return res}))) });
